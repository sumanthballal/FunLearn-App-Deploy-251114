# Backend environment example
# (Backend currently does not require env vars; kept for future use)

# Frontend environment example
# Set the API URL that the frontend should call. For local dev, keep it at localhost.
VITE_API_URL=http://localhost:5000
